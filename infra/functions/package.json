{
  "name": "claudelifter-functions",
  "version": "2.0.0",
  "description": "Azure Functions API for ClaudeLifter workout app",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest --no-coverage"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@azure/cosmos": "^4.2.0",
    "@azure/functions": "^4.6.0",
    "@azure/storage-blob": "^12.26.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.17.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.9",
    "typescript": "~5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
